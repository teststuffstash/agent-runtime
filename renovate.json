{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    "schedule:earlyMondays"
  ],
  "labels": ["dependencies"],
  "rebaseWhen": "auto",
  "separateMinorPatch": true,
  "packageRules": [
    {
      "description": "Devbox/Nix harness pins (goose, opencode, …) — manual review; the lock bump retags the image",
      "matchManagers": ["nix", "devbox"],
      "matchUpdateTypes": ["patch", "minor", "major"],
      "automerge": false,
      "reviewers": ["sootrasmus"]
    },
    {
      "description": "Dockerfile base (jetpackio/devbox) — manual review",
      "matchManagers": ["dockerfile"],
      "matchUpdateTypes": ["patch", "minor", "major"],
      "automerge": false,
      "reviewers": ["sootrasmus"]
    },
    {
      "description": "GitHub Actions — automerge patch/minor",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["patch", "minor"],
      "automerge": true
    }
  ]
}
