# Reviewer-bot approves Renovate `automerge` dep bumps so auto-merge can complete. Thin caller → the
# reusable org workflow in homelab. See homelab FU-014/FU-041.
name: renovate-approve
on:
  pull_request_target:
    types: [opened, reopened, synchronize, labeled]
permissions: {}
jobs:
  approve:
    uses: teststuffstash/homelab/.github/workflows/renovate-approve.reusable.yml@4f6f9260836c7130150e02d6adf7f7965e5eed80 # master
    secrets: inherit
